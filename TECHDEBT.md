# Technical Debt Registry

| ID | Component | Priority | Description | Proposed Solution | Risk | Status |
|----|-----------|----------|-------------|-------------------|------|--------|
| TD-001 | tests | HIGH | The recovered adapter had no executable behavior tests. | Add focused unit tests for scalar conversion, optional fields, nested adapters, list expansion, defaults, and collision errors using RED-GREEN-REFACTOR. | Regressions and incorrect data loads can go unnoticed. | resolved |
| TD-002 | base.py | HIGH | Runtime type introspection and adapter discovery were recovered without tests; `_isadapterclass` and forward-reference handling needed validation. | Replace global module discovery with deterministic schema compilation and explicit namespaces. | Valid schemas may fail at runtime or resolve the wrong class. | resolved |
| TD-003 | adapter.py | HIGH | Nested list flattening and duplicate-key handling were undocumented beyond an example in a docstring. | Define row-based output, Cartesian expansion, empty-list behavior, and typed collision errors in tests and docs. | Silent row loss or data overwrites during ETL. | resolved |
| TD-004 | packaging | MEDIUM | The first public release still requires a maintainer-controlled PyPI/TestPyPI run. | Confirm trusted publishers, run TestPyPI smoke tests, then publish with the manual release workflow. | Broken package metadata or an incorrect public release. | planned |
| TD-005 | compatibility | MEDIUM | Python 3.10–3.13 verification depends on the hosted CI runner. | Keep the CI matrix running test, coverage, Ruff, and strict mypy for each supported interpreter. | Version-specific import or typing failures. | resolved |
| TD-006 | performance | MEDIUM | `adapt()` materializes every output row, so large Cartesian products can consume substantial memory. | Use `max_rows` as a fail-fast guard now; evaluate a future `iter_adapt()` streaming API from benchmark results. | Memory exhaustion or unexpectedly long ETL jobs. | accepted |
